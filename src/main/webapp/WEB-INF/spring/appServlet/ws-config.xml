<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:websocket="http://www.springframework.org/schema/websocket"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc.xsd
       http://www.springframework.org/schema/websocket
       http://www.springframework.org/schema/websocket/spring-websocket.xsd">

	<!-- 웹소켓 핸들러 정의.
		어떤 요청을 어떤 웹소켓 핸들러가 처리할것인지 맵핑
		path: 접속 주소 -->
	<websocket:handlers>
		<!-- 즉, /echo-ws로 접속하면 echoHandler빈을 통해 처리한다고 설정 -->
		<websocket:mapping handler="chattingHandler" path="/chat" />
		<!-- <websocket:sockjs websocket-enabled="true"/> -->
	</websocket:handlers>

	<!-- 웹소켓 핸들러를 상속받아 구현한 클래스들을 빈으로 등록한다. -->
	<bean id="chattingHandler" class="paw.togaether.chat.handler.ChattingHandler" />

	<!-- JS를 이용한 웹소켓 처리 -->
	<!-- <websocket:handlers>
		<websocket:mapping handler="echoHandler" path="/echo.sockjs" />
		<websocket:mapping handler="chatHandler" path="/chat.sockjs" />
		<websocket:sockjs />
	</websocket:handlers> -->
	
	<mvc:default-servlet-handler />
	
<!-- 	<bean -->
<!-- 		class="org.springframework.web.servlet.view.InternalResourceViewResolver"> -->
<!-- 		<property name="prefix" value="/WEB-INF/view/" /> -->
<!-- 		<property name="suffix" value=".jsp" /> -->
<!-- 	</bean> -->

</beans>